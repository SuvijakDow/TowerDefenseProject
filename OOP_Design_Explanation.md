# OOP Design Explanation (Tower Defense Game)

## ทำไมเราถึงใช้ Inheritance, Interfaces, และ Polymorphism ในโครงสร้างนี้
- **Inheritance (การสืบทอด):** เราสร้างคลาสแม่ที่เป็น Abstract Classes เช่น `Enemy` และ `Tower` เพื่อรวบรวมคุณสมบัติ (Fields) และพฤติกรรม (Methods) ที่ใช้งานร่วมกัน เช่น ศัตรูทุกตัวจะต้องมีเลือด (`health`), ความเร็ว (`speed`), และพิกัด (`x`, `y`) รวมทั้งหอคอยทุกแบบจะต้องมีระยะยิง (`range`), พลังโจมตี (`damage`), และเวลาหน่วง (`cooldown`) การใช้ Inheritance ทำให้เราไม่ต้องเขียนโค้ดที่ซ้ำซ้อนในคลาสย่อยอย่าง `SlimeEnemy`, `BatEnemy`, `ArcherTower` และ `PoisonWizardTower`

- **Interfaces (อินเทอร์เฟซ):** เป็นการกำหนดรูปแบบสัญญา (Contract) ว่าคลาสที่นำไปใช้ต้องมีความสามารถนั้นๆ แน่นอน เช่น `Damageable` การันตีว่าคลาสนั้นจะมีเมธอด `takeDamage()` และ `Skillable` รับประกันว่าจะมีเมธอด `useActiveSkill()` สิ่งนี้ทำให้โค้ดมีความยืดหยุ่นสูง ในอนาคตถ้ามีสิ่งของใดๆ ในเกมที่พังได้ (เช่น ต้นไม้หรือกำแพง) ก็สามารถให้ implement `Damageable` ได้ทันที

- **Polymorphism (พหุสัณฐาน):** เราสามารถจัดการกับออบเจกต์ที่ต่างกันผ่านรูปแบบเดียวกันได้ เช่น ในเมธอด `attack(List<Enemy> enemies)` ของคลาส `Tower` เราสามารถรับลิสต์ที่รวมทั้ง `SlimeEnemy` และ `BatEnemy` เข้ามาประมวลผลได้พร้อมกันโดยมองว่าเป็น `Enemy` เพียงอย่างเดียว ซึ่งทำให้การขยายสเกลระบบ (Scalability) ทำได้ง่ายเมื่อมีการเพิ่มศัตรูประเภทใหม่เข้ามา

## การใช้ Access Modifiers ปกป้องข้อมูลของเราอย่างไร
- เราใช้กระบวนการ Encapsulation โดยการกำหนดตัวแปร (Fields) ส่วนใหญ่ให้เป็น `private` หรือ `protected` 
- การใช้ `protected` ทำให้คลาสแม่เช่น `Enemy` สามารถแชร์ข้อมูลให้กับคลาสลูก (`SlimeEnemy`) เพื่อนำไปคำนวณการเคลื่อนที่ได้โดยตรง แต่บล็อกไม่ให้คลาสอื่นๆ หรือระบบเกมส่วนอื่นเข้ามาแก้ค่าเหล่านี้มั่วซั่วได้
- เราจะอนุญาตให้ดึงข้อมูลหรือตั้งค่าผ่าน `getters` และ `setters` เท่านั้น ซึ่งข้อดีคือเราสามารถแอบเพิ่มเงื่อนไขตรวจสอบความถูกต้องได้ เช่น ในฟังก์ชัน `takeDamage()` เราดักจับไม่ให้ `currentHealth` มีค่าน้อยกว่าศูนย์เป็นต้น

## การแยก Logic ออกมาส่งผลดีต่อการทำ JUnit Testing อย่างไร
- แพ็กเกจ `logic` ถูกบังคับให้มีแต่โค้ดของภาษา Java ล้วนๆ (Pure Java) โดยปราศจากการเรียกใช้ JavaFX, GUI หรือโค้ดโหลดรูปภาพใดๆ (Decoupling) 
- ข้อดีของการทำแบบนี้คือ **Unit Testing มีประสิทธิภาพสูงมาก** เพราะเทสต์สามารถรันเสร็จได้ภายในเสี้ยววินาที เราสามารถจำลองให้หอคอยยิงศัตรู จำลองการอัปเกรด หรือจำลองให้ศัตรูเดินตาม Waypoint ได้ทันที แล้ว assert ผลลัพธ์ว่าถูกต้องหรือไม่ โดยไม่ต้องสนใจว่าหน้าจอจะกระตุก หรือต้องจำลองหน้าต่างแอปพลิเคชันขึ้นมา ทำให้เรามั่นใจว่าระบบแกนหลักของเกมเราบั๊กน้อยและเสถียรที่สุด
