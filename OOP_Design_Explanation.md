# คำอธิบายการออกแบบเชิงวัตถุ (OOP) สำหรับ Core Logic ของเกม 2D Tower Defense

เอกสารนี้อธิบายแนวคิดการออกแบบคลาสพื้นฐานในแพ็กเกจ `logic` ซึ่งประกอบด้วย `Damageable`, `Upgradable`, `Enemy`, และ `Tower` โดยเน้นหลักการ OOP ที่ชัดเจนและพร้อมต่อยอดในระดับโปรเจกต์มหาวิทยาลัย

## 1) ภาพรวมการออกแบบตรรกะ (Overall Logic Design)

การออกแบบแบ่งโครงสร้างเป็น 2 แกนหลักของเกม Tower Defense:

1. ฝั่งศัตรู (Enemy Side)
   - กำหนดพฤติกรรมร่วมของศัตรูผ่านคลาสนามธรรม `Enemy`
   - ศัตรูทุกตัวต้องรับดาเมจได้ (`Damageable`)
   - ศัตรูแต่ละชนิดกำหนดการเคลื่อนที่เองผ่านเมธอด `move()`

2. ฝั่งป้อม (Tower Side)
   - กำหนดพฤติกรรมร่วมของป้อมผ่านคลาสนามธรรม `Tower`
   - ป้อมทุกตัวสามารถอัปเกรดได้ (`Upgradable`)
   - ป้อมแต่ละชนิดกำหนดวิธีโจมตีเองผ่านเมธอด `attack(List<Enemy> targets)`

ผลลัพธ์คือโครงสร้างแกนเกมที่แยก “คุณสมบัติร่วม” ออกจาก “พฤติกรรมเฉพาะชนิด” อย่างชัดเจน ทำให้ขยายเกมได้ง่าย เช่นเพิ่มศัตรูชนิดใหม่หรือป้อมชนิดใหม่โดยไม่กระทบโค้ดเดิมมาก

## 2) เหตุผลในการใช้ Inheritance (สืบทอดคลาส) สำหรับ Enemy และ Tower

### ทำไมใช้คลาสนามธรรม `Enemy`

- ศัตรูทุกประเภทมีข้อมูลพื้นฐานร่วมกันเหมือนกัน: `health`, `baseSpeed`, `x`, `y`
- พฤติกรรมบางส่วนควรใช้ร่วมกันได้ทันที เช่น `takeDamage(...)`, `isAlive()`, getter/setter
- แต่พฤติกรรมการเคลื่อนที่ต้องแตกต่างกัน เช่น ศัตรูวิ่งตรง, ซิกแซก, หรือปรับตามเส้นทาง จึงประกาศ `move()` เป็น abstract

สรุป: ใช้ `Enemy` เพื่อลดโค้ดซ้ำ (reuse) และบังคับให้ลูกคลาสใส่ logic การเดินที่เหมาะกับชนิดของตัวเอง

### ทำไมใช้คลาสนามธรรม `Tower`

- ป้อมทุกประเภทมีสถานะร่วม: `damage`, `range`, `attackCooldown`, `x`, `y`
- มีพฤติกรรมร่วมได้ เช่นการอัปเกรดพื้นฐานใน `upgrade()`
- แต่รูปแบบโจมตีขึ้นอยู่กับชนิดป้อม เช่นยิงเป้าหมายเดี่ยว, ยิงหมู่, ยิงชะลอความเร็ว จึงประกาศ `attack(...)` เป็น abstract

สรุป: ใช้ `Tower` เพื่อให้การเพิ่มป้อมใหม่ทำได้เป็นระบบ โดยไม่ต้องเขียนโครงสร้างพื้นฐานซ้ำในทุกคลาส

## 3) เหตุผลในการใช้ Interface (Damageable, Upgradable)

### `Damageable`

- เป็นสัญญา (contract) ว่า object ที่ implement ต้องรองรับ `takeDamage(int amount)`
- ข้อดีสำคัญคือ “ไม่ผูกกับสายสืบทอดเดียว” วัตถุอื่นที่ไม่ใช่ `Enemy` ก็ implement ได้ เช่น Base/Core ของผู้เล่น หรือสิ่งกีดขวางที่พังได้
- ช่วยให้ระบบ combat ทำงานกับ abstraction แทนการยึดคลาสจริง ทำให้ยืดหยุ่นสูง

### `Upgradable`

- เป็นสัญญาว่าวัตถุรองรับการ `upgrade()`
- ไม่จำกัดเฉพาะ `Tower` ในอนาคตอาจให้ระบบอื่นอัปเกรดได้ เช่นกับดัก (Trap), กำแพง (Wall), หรือสกิลพิเศษ
- ทำให้ระบบจัดการอัปเกรดสามารถเขียนกับ interface กลางได้

สรุป: Interface ช่วยแยก “ความสามารถ” ออกจาก “สายตระกูลคลาส” ส่งเสริมการออกแบบที่ยืดหยุ่นและทดสอบง่าย

## 4) การใช้ Polymorphism ผ่าน abstract methods (`move`, `attack`)

Polymorphism เกิดจากการอ้างอิงด้วยชนิดแม่ แต่เรียกใช้พฤติกรรมจริงของชนิดลูก

ตัวอย่างแนวคิด:

- เก็บศัตรูหลายชนิดไว้ใน `List<Enemy>` แล้ววนเรียก `enemy.move()`  
  แม้จะอ้างเป็น `Enemy` เหมือนกัน แต่แต่ละ object จะเคลื่อนที่ตาม implementation ของคลาสลูก

- เก็บป้อมหลายชนิดไว้ใน `List<Tower>` แล้วเรียก `tower.attack(enemies)`  
  แต่ละป้อมจะใช้ตรรกะยิงของตัวเองโดยอัตโนมัติ

ผลลัพธ์คือ game loop หลักเขียนแบบทั่วไปได้ (generic loop) และลดการใช้เงื่อนไข if-else/switch ตามชนิดวัตถุจำนวนมาก

## 5) เหตุผลของ Access Modifiers (`protected`, `private`, `public`)

### ทำไมฟิลด์หลักใน `Enemy` และ `Tower` ใช้ `protected`

- ข้อกำหนดโปรเจกต์ต้องการให้คลาสลูกเข้าถึง state สำคัญได้โดยตรงเมื่อจำเป็น
- `protected` ทำให้คลาสลูกในสายสืบทอดอ่าน/ปรับค่าได้สะดวกกว่าการบังคับผ่านเมธอดทุกครั้ง
- เหมาะกับคลาสฐานของเกมที่ต้องการ extensibility สำหรับชนิดลูกหลายแบบ

### ทำไมใช้ `private` กับเมธอดตรวจสอบค่า (validation helper)

- เมธอดช่วยตรวจค่าเช่น `requireNonNegative(...)` เป็นรายละเอียดภายในของคลาส ไม่ควรถูกเรียกจากภายนอก
- `private` ลดการพึ่งพาจากภายนอก (reduce coupling) และป้องกันการใช้งานผิดวัตถุประสงค์
- ทำให้ปรับแก้ implementation ภายในได้โดยไม่กระทบ API สาธารณะ

### ทำไม getter/setter และเมธอดหลักใช้ `public`

- จำเป็นต่อการใช้งานจากระบบเกมส่วนอื่น เช่น service/game loop/test
- เปิดเผยเฉพาะพฤติกรรมที่ควรใช้งานภายนอก และควบคุมความถูกต้องของข้อมูลผ่าน setter/validation

สรุป: โครงสร้าง modifier นี้รักษาสมดุลระหว่าง “การปกปิดข้อมูล” และ “ความสามารถในการขยายคลาส”

## 6) ประโยชน์เชิงสถาปัตยกรรมสำหรับงานนำเสนอ

- โครงสร้างชัดเจน: interface = ความสามารถ, abstract class = แกนร่วม, subclass = พฤติกรรมเฉพาะ
- ลดโค้ดซ้ำ: state และ logic พื้นฐานอยู่ที่คลาสฐาน
- ขยายง่าย: เพิ่มชนิดศัตรู/ป้อมใหม่โดยไม่แก้แกนหลักมาก
- รองรับการทดสอบ: ทดสอบตามสัญญา interface และพฤติกรรม polymorphic ได้สะดวก
- พร้อมต่อยอด: เชื่อมกับระบบ pathfinding, wave manager, targeting strategy ได้ทันทีในเฟสถัดไป

## 7) การต่อยอดด้วย Subclasses จริงในเกม (Basic/Tank/Boss และ Sniper/Bomb/Mage)

### 7.1 การสาธิต Inheritance อย่างชัดเจน

- `BasicEnemy`, `TankEnemy`, `BossEnemy` สืบทอดจาก `Enemy` ทำให้ทุกคลาสใช้โครงสร้างร่วมเดียวกัน (`health`, `baseSpeed`, `x`, `y`) และพฤติกรรมร่วม (`takeDamage`, `isAlive`) ได้ทันที
- `SniperTower`, `BombTower`, `MageTower` สืบทอดจาก `Tower` จึงใช้สถานะร่วม (`damage`, `range`, `attackCooldown`, `x`, `y`) และนโยบายอัปเกรดพื้นฐานร่วมกัน
- การแยก “ส่วนที่เหมือนกันไว้ในคลาสแม่” และ “ส่วนที่ต่างกันไว้ในคลาสลูก” ช่วยลดโค้ดซ้ำและทำให้โปรเจกต์ขยายได้ง่ายเมื่อเพิ่มยูนิตชนิดใหม่

### 7.2 การ Override `move()` และ `attack()` เพื่อสาธิต Polymorphism

- ฝั่งศัตรู: ทั้ง 3 คลาส override `move()` โดย engine สามารถเก็บเป็น `List<Enemy>` แล้วเรียก `move()` แบบเดียวกันได้ แต่ผลการทำงานขึ้นอยู่กับชนิดจริงของ object
- ฝั่งป้อม: ทั้ง 3 คลาส override `attack(List<Enemy>)` ด้วยตรรกะต่างกันชัดเจน
  - `SniperTower` ยิงเป้าหมายแรกในระยะ (single target)
  - `BombTower` เลือกเป้าหมายหลักแล้วกระจายดาเมจรอบจุดนั้น (AoE)
  - `MageTower` โจมตีปกติในระยะ และมีสกิลแยกต่างหาก
- นี่คือ polymorphism แบบใช้งานจริง: โค้ดระดับระบบเรียกเมธอดชื่อเดียวกัน แต่ได้พฤติกรรมเฉพาะคลาสโดยอัตโนมัติ

### 7.3 การเพิ่ม `Skillable` ให้ `MageTower` และความยืดหยุ่นของ OOP

- `Skillable` คือ interface ความสามารถเฉพาะทาง (active skill) ที่ไม่จำเป็นต้องบังคับให้ทุกป้อมมี
- `MageTower` จึงเป็นตัวอย่างของการผสม “Inheritance + Interface”:
  - ได้โครงสร้างป้อมพื้นฐานจาก `Tower`
  - ได้ความสามารถพิเศษเพิ่มเติมจาก `Skillable`
- ข้อดีเชิงออกแบบคือยืดหยุ่นสูง: ในอนาคตสามารถให้คลาสอื่น implement `Skillable` ได้ทันทีโดยไม่ต้องเปลี่ยนโครงสร้าง `Tower` ทั้งระบบ

## 8) บทบาทของ `GameManager` และความพร้อมสำหรับการทดสอบ

### 8.1 พลังของ Polymorphism ใน `update()`

- `GameManager` ทำงานกับ `List<Enemy>` และ `List<Tower>` ในระดับชนิดแม่ (abstraction) เท่านั้น
- ใน game tick เดียวกัน เมธอด `update()` เรียก `enemy.move()` กับศัตรูทุกตัว และ `tower.attack(activeEnemies)` กับป้อมทุกตัว โดยไม่ต้องรู้ว่า object จริงเป็น `BasicEnemy`, `BossEnemy`, `SniperTower`, `BombTower` หรือ `MageTower`
- นี่คือ polymorphism ที่ใช้ได้จริงในระบบเกม: โค้ดควบคุมกลางสั้น ชัด และขยายได้ง่าย เพราะพฤติกรรมเฉพาะถูกจัดการโดยการ override ในคลาสลูก
- กรณีพิเศษของ `BossEnemy` (แตกตัวเมื่อตาย) ถูกแยกเป็นเงื่อนไขเฉพาะจุดในขั้นตอนประมวลผลศัตรูตาย ทำให้กติกาพิเศษอยู่ในระบบกลางอย่างเป็นระเบียบ

### 8.2 การแยก Logic ออกจาก GUI ทำให้ทดสอบด้วย JUnit ได้เต็มรูปแบบ

- `GameManager` และคลาสในแพ็กเกจ `logic` ไม่มีการพึ่งพา JavaFX/GUI เลย จึงเป็น pure business logic
- สามารถเขียน JUnit test ได้ตรงๆ เช่น:
  - ทดสอบว่า `placeTower(...)` หักเงินถูกต้องและโยน exception เมื่อเงินไม่พอ
  - ทดสอบว่า `update()` เคลื่อนศัตรู, สั่งป้อมโจมตี, ลด `baseHealth` เมื่อศัตรูถึงปลายทาง
  - ทดสอบว่าเมื่อ `BossEnemy` ตายจะเกิดลูก 3 ตัวตามกติกา
- ผลลัพธ์คือวงจรพัฒนาแบบ test-first/test-driven ทำได้ง่าย ลดความเสี่ยงก่อนเชื่อมต่อชั้นภาพ (visual layer) ในเฟสถัดไป

## 9) JUnit Testing Strategy

### 9.1 ทำไมต้องมีชุดทดสอบตามหัวข้อเหล่านี้

- เราเลือกทดสอบตาม “กติกาหลักของเกม” โดยตรง ได้แก่สถานะเริ่มต้น, เศรษฐกิจการวางป้อม, การ spawn ศัตรู และลำดับการทำงานของ `update()`
- ชุดทดสอบครอบคลุมทั้งกรณีปกติและกรณีผิดพลาด (เช่นเงินไม่พอ) เพื่อยืนยันว่า logic ไม่พังใน edge case สำคัญ
- กลไกเฉพาะของเกม เช่นบอสแตกตัว ถูกทดสอบแยกชัดเจน เพื่อป้องกัน regression เมื่อมีการแก้ระบบในอนาคต

### 9.2 ทดสอบ Core Logic ได้โดยไม่ต้องมี GUI อย่างไร

- ทุกคลาสใน `logic` ไม่พึ่ง JavaFX จึงสร้าง object และเรียกเมธอดได้ทันทีใน JUnit
- การทดสอบเน้นตรวจ state change หลังเรียกเมธอด เช่น:
  - เงินผู้เล่นเพิ่ม/ลดถูกต้อง
  - ศัตรูถูกลบออกเมื่อถึงฐานหรือเมื่อตาย
  - `baseHealth` ลดลงตามกติกา
  - รายชื่อศัตรู active เปลี่ยนตามผลของ tick
- แนวทางนี้ทำให้ตรวจสอบกติกาเกมได้เร็วและแม่นยำ โดยไม่ต้องรันหน้าจอหรือระบบแสดงผล

### 9.3 วิธีอ่านผลทดสอบเพื่อยืนยันความถูกต้องของระบบ

- ถ้าเทสทั้งหมดผ่าน แปลว่า game loop หลักและกติกาสำคัญยังทำงานถูกต้องตามสเปก
- ถ้าเทสบางตัวล้มเหลว ให้ดูชื่อเทสที่ล้มเพื่อระบุได้ทันทีว่าปัญหาอยู่ที่โมดูลใด (เศรษฐกิจ, การโจมตี, การเคลื่อนที่, หรือกลไกบอส)
- การมีเทสแยกเป็นรายพฤติกรรมช่วยให้แก้ไขได้ตรงจุดและมั่นใจว่าการแก้โค้ดไม่ทำให้กติกาส่วนอื่นเสียตามมา
